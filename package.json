{
  "name": "gutu-lib-ui-query",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/ui-query build",
    "typecheck": "bun run --cwd framework/libraries/ui-query typecheck",
    "lint": "bun run --cwd framework/libraries/ui-query lint",
    "test": "bun run --cwd framework/libraries/ui-query test",
    "test:unit": "bun run --cwd framework/libraries/ui-query test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
